---
# Runs in the LIVE system (not in the target chroot) so grub-probe
# can see real block devices instead of the overlayfs root.
# Handles grub-install + grub.cfg for both BIOS and EFI targets.
dontChroot: true
timeout: 120
script:
    - "/usr/bin/bash /usr/local/bin/vertex-bootsetup-host"
